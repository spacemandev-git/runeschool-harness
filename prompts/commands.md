# Commands

The `act` tool lists the command names supplied by the active world adapter. Submit one command at a
time, inspect its acknowledgement, and observe again before assuming a multi-step effect completed.
