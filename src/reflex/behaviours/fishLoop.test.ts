import{describe,expect,test}from'bun:test';import{FISH_LOOP}from'./fishLoop.ts';import{FakeContext,makeSnapshot}from'../testing.ts';
describe('fish-loop',()=>{test('walks to a spot then fishes adjacent',async()=>{const spot={id:8,kind:'npc' as const,name:'Fishing spot',at:{x:3,z:0,level:0},distance:3,lastSeenTick:0};const c=new FakeContext(makeSnapshot({self:{at:{x:0,z:0,level:0}},nearby:[spot]}));const b=FISH_LOOP.create({spot:8,option:'net',until:{count:2},then:'stop'});await b.start(c);expect(c.intents[0]?.type).toBe('walk');c.setSnapshot(makeSnapshot({self:{at:{x:2,z:0,level:0}},nearby:[{...spot,distance:1}]}));await b.step(c);expect(c.intents[1]).toMatchObject({type:'fish',data:{spot:8,option:'net'}});});});
