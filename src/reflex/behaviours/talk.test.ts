import{describe,expect,test}from'bun:test';import{TALK}from'./talk.ts';import{FakeContext,makeEvent}from'../testing.ts';
describe('talk',()=>{test('advances ordinary and choice nodes and completes',async()=>{const c=new FakeContext();const b=TALK.create({dialogue:'quest',choices:[1]});await b.start(c);c.inject(makeEvent('dialogue-node',{entity:1,dialogue:'quest',nodeId:'a',kind:'npc',speakerTag:'n',text:'hi'}));await b.step(c);c.clearEvents();c.inject(makeEvent('dialogue-node',{entity:1,dialogue:'quest',nodeId:'b',kind:'choice',options:['x','y']}));await b.step(c);expect(c.intents[2]?.data).toEqual({choice:1});c.clearEvents();c.inject(makeEvent('dialogue-ended',{entity:1,dialogue:'quest'}));expect((await b.step(c)).state).toBe('done');});});
