import{describe,expect,test}from'bun:test';import{GATHER_LOOP}from'./gatherLoop.ts';import{FakeContext,makeEvent,makeSnapshot}from'../testing.ts';
describe('gather-loop',()=>{test('inventory-full drops gathered stacks one per pulse',async()=>{const snap=makeSnapshot({inventoryFree:0,inventory:[{slot:0,item:1511,name:'Logs',amount:1},{slot:1,item:1511,name:'Logs',amount:1}]});const c=new FakeContext(snap);c.inject(makeEvent('gathered',{entity:1,node:'tree',item:1511,xp:10}),makeEvent('item-added',{entity:1,item:1511,amount:1,overflow:0}));const b=GATHER_LOOP.create({node:'tree',until:'inventory-full',then:'drop-all'});expect((await b.start(c)).state).toBe('running');expect(c.intents[0]).toMatchObject({type:'drop',data:{slot:0}});c.clearEvents();await b.step(c);expect(c.intents[1]).toMatchObject({type:'drop',data:{slot:1}});});});
